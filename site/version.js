// ONE version constant. Classic script (not a module) so sw.js can importScripts it
// and app.js can read it as a global. Bump on every deploy: YYYY-MM-DD + letter.
self.APP_VERSION = "2026-09-21a";
