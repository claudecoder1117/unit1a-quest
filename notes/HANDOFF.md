# HANDOFF — 2026-09-17 12:30 (school-hours hard-stop check)

## State
- Live: https://claudecoder1117.github.io/unit1a-quest/ (Pages via Actions, deploys `site/` on every push to main; CI runs `node --test` first).
- main = 7710490. Build waves 0–5 + QA rounds 1–2 + final scorecard all committed and pushed.
- `node --test` → 1229 pass, 0 fail. Precache list current (116 files).
- No workflow running. The wow-loop (wf_750782cb-3d9) completed at ~04:10; nothing was cut off.
- Final scorecard (`notes/S9-SCORECARD.md`): S9 10/10 pass, overall wow 8/10.

## Next pass (not started — quiet window until 14:15; do not launch workflows before then)
The five rough edges a student meets first (details + evidence in S9-SCORECARD.md):
1. Correct answers on a new skill drop provisional Readiness and can list the skill as "weak" (m_shown = m·min(1,n/5)). Show "just started" for n < 3 with no wrong answer.
2. Placement item 1 (T-notation) says "labelled in the figure" but emits `figure: null`. Draw the small fan or reword.
3. Today's Page on a 375×667 phone: answer box below the fold (three stacked headers). Compact the run header.
4. Summary family tiles read bronze/silver right after a GOLD card; "Quadratics, a > 1" label wraps with an orphan comma.
5. Home shows the Mock twice (CTA + 32 px text link); placement summary still uses lore module names ("Factor Forge", "ASN Arena", "Lexicon").
Also open: cold-boot budgets (3G paint 2.8 s vs 2.5), Lighthouse not run, sound never heard (quiet hours), two figure wedges 42 px tall at 375.
